# ClickUp setup

Create a ClickUp personal/API token with access to the intended workspace. Set `CLICKUP_API_TOKEN` and `CLICKUP_TEAM_ID`. The adapter is optional and reports `DISABLED` when absent; startup never depends on ClickUp.

