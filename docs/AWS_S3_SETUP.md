# AWS S3 setup

Create a private bucket with encryption, blocked public access, and a least-privilege IAM identity. Set `AWS_REGION`, `AWS_S3_BUCKET`, `AWS_ACCESS_KEY_ID`, and `AWS_SECRET_ACCESS_KEY`; then set `STORAGE_PROVIDER=s3`. The default `local` provider stores files under `storage/local`.

