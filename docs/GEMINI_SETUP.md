# Gemini setup

Create an API key in Google AI Studio, place it in `GEMINI_API_KEY`, and select the deployed model via `GEMINI_MODEL`. Gemini remains disabled when no key exists. Never expose the key to the browser.

