# MongoDB Atlas setup

Create an Atlas project, M10+ cluster for vector search, least-privilege database user, and network access rule. Set `MONGODB_URI` with database `barabari`. Collections store metadata and object references; binaries belong in S3/local storage.

